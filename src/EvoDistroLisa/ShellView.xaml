<Window 
        x:Class="EvoDistroLisa.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid Margin="8">
            <Image Name="Image" Margin="8"/>
            <TextBlock Name="Speed" FontSize="16" FontWeight="Bold" Foreground="Black" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="8"/>
        </Grid>
    </Grid>
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="xxx"/>
            <MenuItem Header="yyy"/>
        </ContextMenu>
    </Window.ContextMenu>
</Window>